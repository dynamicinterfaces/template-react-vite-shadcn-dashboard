import { Label } from './label';

const meta = { component: Label };
export default meta;

export const Default = { args: { children: 'Email address' } };
export const WithHtmlFor = { args: { children: 'Username', htmlFor: 'username' } };
